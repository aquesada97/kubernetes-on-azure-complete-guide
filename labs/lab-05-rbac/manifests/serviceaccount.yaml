apiVersion: v1
kind: ServiceAccount
metadata:
  name: lab05-sa
  namespace: lab05
  labels:
    lab: lab05
  annotations:
    description: "ServiceAccount for Lab 05 RBAC demonstration"
automountServiceAccountToken: true    # Mount SA token into pods using this SA
